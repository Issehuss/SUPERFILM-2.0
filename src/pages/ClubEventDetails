import { useParams, Link } from 'react-router-dom';

function ClubEventDetails() {
  const { id, eventSlug } = useParams();

  return (
    <div className="min-h-screen bg-black text-white p-10">
      <div className="max-w-4xl mx-auto space-y-6">
        <h1 className="text-4xl font-bold text-yellow-400">🎞️ Past Event</h1>
        <p className="text-zinc-300">Club ID: {id}</p>
        <p className="text-zinc-300">Event: {eventSlug}</p>

        <Link
          to={`/club/${id}`}
          className="inline-block mt-4 bg-yellow-500 hover:bg-yellow-400 text-black px-6 py-2 rounded-full font-semibold"
        >
          ← Back to Club
        </Link>

        {/* TODO: Fetch real event data by slug and render it here */}
      </div>
    </div>
  );
}

export default ClubEventDetails;
